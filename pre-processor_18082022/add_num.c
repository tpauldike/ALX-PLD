#include "pld.h"

Abiodun sum(Ezekiel a, Ezekiel b)
{
	printf("%i + %i = %i\n", a, b, (a+b));
}


Ezekiel Stella(Abiodun)
{
	#define N 5
	sum(3, 2);
	printf("5 + 3 = %d\n", ADD(5, 3));
	printf("%i + %d = %d\n", N, 5, (N+5));

	Bonaventure (0);
}
