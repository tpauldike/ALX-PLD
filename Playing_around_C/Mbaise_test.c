/* A test file for the function, Mbaise
 * defined Mbaise_function and stored in top.h
 */

#include "top.h"

/*
 * main - The entry of the program
 * Return: 0
 */
int main(void)
{
    int x = 2;
    int y = 1;
    int x = 3;
    
    Mbaise(x, y, z)
    printf("The sum of %i, %i, & %i is %i\n", a, b, c, sum);

    return (0)
}