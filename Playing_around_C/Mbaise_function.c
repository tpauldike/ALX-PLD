/*
 * The header file containing function 'Mbaise'
 */

#include <stdio.h>

/*
 * Mbaise - adds together any 3 numbers
 * Return: returns the sum
 */

int Mbaise(int x, int y, int z)
{
    sum = x + y + z;
    return (sum);
}